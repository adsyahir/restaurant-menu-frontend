# Copy to .env (git-ignored) and fill in.
#   cp .env.example .env

# Backend public API the browser calls.
NUXT_PUBLIC_API_BASE=https://api.example.com

# The domain this frontend is served on — k8s/ingress.yaml reads it via envsubst.
FRONTEND_HOST=app.example.com
